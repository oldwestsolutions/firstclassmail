import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Toaster } from 'react-hot-toast'
import { AuthProvider } from '@/contexts/AuthContext'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'FirstClass Mail - Professional Virtual Mailbox Services for Everyone',
  description: 'Professional virtual mailbox services, mail scanning, forwarding, and secure mail handling for individuals, small businesses, and entrepreneurs',
  keywords: 'virtual mailbox, mail scanning, mail forwarding, professional address, small business, digital nomad, mail management',
  viewport: 'width=device-width, initial-scale=1, maximum-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <AuthProvider>
          {children}
          <Toaster position="top-right" />
        </AuthProvider>
      </body>
    </html>
  )
}
